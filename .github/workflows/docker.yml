name: Create and publish a Docker image

on:
  push:
    branches: ["main"]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=,format=short
            type=raw,value=main

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@f2a1d5e99d037542a71f64918e516c093c6f3fc4
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::272095582125:role/github-actions-lightsail-deploy
          aws-region: us-east-2
      
      - name: Force Lightsail redeployment
        run: |
          # Use the git SHA tag for this specific build
          IMAGE_TAG="${{ github.sha }}"
          IMAGE_TAG_SHORT="${IMAGE_TAG:0:7}"
          
          # Get current deployment configuration
          CURRENT_DEPLOYMENT=$(aws lightsail get-container-services \
            --service-name sc-market \
            --region us-east-2 \
            --query 'containerServices[0].currentDeployment' \
            --output json)
          
          # Extract containers config and update discord image to specific SHA tag
          CONTAINERS=$(echo $CURRENT_DEPLOYMENT | jq --arg tag "$IMAGE_TAG_SHORT" '.containers | 
            .discord.image = "ghcr.io/sc-market/scmarketbot:" + $tag')
          
          # Extract public endpoint config
          PUBLIC_ENDPOINT=$(echo $CURRENT_DEPLOYMENT | jq '.publicEndpoint')
          
          # Create new deployment with specific image tag
          aws lightsail create-container-service-deployment \
            --service-name sc-market \
            --region us-east-2 \
            --containers "$CONTAINERS" \
            --public-endpoint "$PUBLIC_ENDPOINT"
